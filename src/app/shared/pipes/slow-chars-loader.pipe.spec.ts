import { SlowCharsLoaderPipe } from './slow-chars-loader.pipe';

describe('SlowCharsLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SlowCharsLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
